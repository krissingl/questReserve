# QuestReserve Frontend — environment variables
# Copy this file to .env.local and fill in values for local development.

# Backend API base URL — must include the /api prefix, no trailing slash.
# The backend defaults to port 3001 with all routes mounted under /api.
VITE_API_URL=http://localhost:3001/api
